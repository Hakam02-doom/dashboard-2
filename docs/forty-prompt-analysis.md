# Forty-question analysis

New analyses use 40 independently searched and assessed buyer questions: 10 each for Discovery, Comparison, Buying decisions, and Use cases. Existing percentage charts remain on a 0–100 scale; question progress uses 40 as its denominator.

The Netflix onboarding failure happened before any provider requests: its homepage exceeded the reader's 2 MB limit. The reader now permits 8 MB with a 12-second deadline. Oversized pages, HTTP 403/429/5xx responses and temporary connection errors can use verified indexed snippets. Invalid/private addresses never enter that fallback. Unexpected worker errors are logged with their job ID for diagnosis.

The user approved a $30 total conservative reservation ceiling on September 24, 2026. Both the application and the atomic database reservation enforce it. New runs check for at least 84 calls of headroom (four planning requests and two requests per question) before discarding the current report. Provider errors and question repairs can need additional calls, which remain bounded by the same ceiling. Reservations are not the actual provider bill.

Validation: the live Netflix reader returned its correct name and description in 7.85 seconds. Automated checks exercise all 40 searches and evidence assessments, repair an incomplete planning batch, resume without repeating completed calls, preserve safe URL validation, and open early results after four measurements. Live production results are recorded after deployment.
